extends layout 
include mixins/crearProyecto
block contenido 
  .contenido-principal 
    h1= title
    if errores 
      each error in errores 
        .alerta.error= error.texto
    +crearProyecto(proyecto)